export const config = {
  siteMeta: {
    title: 'おまかセトリ',
    teamName: '',
    description: '',
  },
  siteRoot:
    process.env.NODE_ENV === 'production' ? 'https://omakasetli.com' : 'http://localhost:3000',
  themeColor: '#fff',
}
